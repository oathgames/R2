# Source code
merlin/
*.go
go.mod
go.sum
build.ps1

# Binaries
*.exe
*.exe~
.claude/tools/Merlin*
.claude/tools/ffmpeg*
.claude/tools/ffprobe*

# Config with secrets (example template IS tracked)
.claude/tools/merlin-config.json
.claude/tools/.merlin-config-*
.claude/tools/.merlin-tokens
.claude/tools/.merlin-tokens-*
.claude/tools/.autocmo-*
.claude/tools/autocmo-*
.claude/tools/AutoCMO*
!.claude/tools/merlin-config.example.json

# Runtime fact-binding session artifacts (Security Rule 12). The HMAC
# key + signed envelope log are per-session and MUST NOT reach git —
# they contain keys/signatures derived from the user's vault state.
.claude/tools/.merlin-facts-key.*
.claude/tools/.merlin-facts.*
.claude/tools/.merlin-vault*
.claude/tools/.merlin-ratelimit*
.claude/tools/.merlin-audit*
.merlin-ref-claim-attempted

# Output
results/

# Temp files
tmp_cmd*.json
merlin-music-pack.zip
*.update
*.backup
.merlin-backup/

# Brand-specific assets (users bring their own)
assets/brands/*/
!assets/brands/example/
assets/music/*.mp3
assets/music/*.wav
assets/music/*.ogg
assets/music/*.flac
assets/music/*.zip

# Quality benchmarks (brand-specific)
extract_colors.ps1

# TTS model cache (fetched from HF on first voice-output use, ~92 MB)
.claude/tools/kokoro-cache/

# Node/Electron
node_modules/
dist/
.merlin-key
.env

# Wrangler local deploy cache (build artefacts, never source)
.wrangler/

# Certificates & signing keys (NEVER commit)
*.cer
*.p12
*.pem
*.key
*.csr
*.mobileprovision
*.provisionprofile

# OS
.DS_Store
Thumbs.db
.merlin-icon.png
.r2-key
.merlin-machine-id
.merlin-key-usage.json
.merlin-trial
.merlin-subscription
.merlin-wisdom.json
.merlin-state.json
.merlin-threads.json
.merlin-briefing.json
.merlin-license-token
.merlin-referral-bonus
.merlin-errors.log
.merlin-errors.log.old

# Session-review artifacts (tools/review-session.sh output)
.session-review.md
.session-review.json
.session-review.json.err
